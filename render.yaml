services:
  - type: web
    name: danganronpa-ttrpg
    runtime: node
    plan: free
    buildCommand: echo 'Build complete'
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
